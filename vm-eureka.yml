version: "2"
services:
  vm-eureka:
    image: registry.cn-shenzhen.aliyuncs.com/godaner/vm-eureka
    ports:
      - "1110:1110"
    network_mode: host
    environment:
      - spring.profiles.active=prod00
    volumes:
      - /usr/local:/usr/local
